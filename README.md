# J2ME Telegram Client (Nokia Retro Project) 📱

Проект представляет собой связку из J2ME-клиента для старых телефонов (например, Nokia C5-00) и Python-сервера (прокси), который позволяет использовать Telegram на устройствах с поддержкой Java.

## 🏗 Структура проекта

- `/client` — Исходный код Java-клиента (J2ME, MIDP 2.0).
- `/server` — Python-сервер на базе Quart и Telethon.
- `.github/workflows` — Автоматическая сборка `.jar` и `.jad` файлов через GitHub Actions.

## 🚀 Как запустить

### 1. Сервер (Python)
Сервер выступает в роли прокси-шлюза между Telegram API и Java-клиентом.
1. Перейдите в папку `server`.
2. Установите зависимости: `pip install -r requirements.txt`.
3. Настройте `config.py` (API_ID, API_HASH).
4. Запустите: `python server.py`.

### 2. Клиент (Java)
1. Соберите проект или скачайте готовый `.jar` из разделов Artifacts в GitHub Actions.
2. Установите на телефон.
3. При запуске введите URL вашего сервера (например, через Serveo или статический IP).

## ✨ Возможности
- ✅ Авторизация по номеру телефона.
- ✅ Просмотр списка чатов.
- ✅ Чтение и отправка сообщений.
- ✅ Отображение фотографий в чате.
- ✅ Поддержка кириллицы (UTF-8).
- ✅ Отправка фото из галереи телефона (JSR 75).

## 🛠 Технологии
- **Java ME**: MIDP 2.0, CLDC 1.1, JSR 75 (File Connection).
- **Python 3.10+**: Quart, Telethon, Pillow.
- **GitHub Actions**: Автоматизация сборки JAR.

---
*Разработано для любителей ретро-техники и фанатов Nokia.*
